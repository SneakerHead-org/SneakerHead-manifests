# Order PostgreSQL ConfigMap - prod
apiVersion: v1
kind: ConfigMap
metadata:
  name: order-postgres-config
  namespace: sneakerhead-prod
  labels:
    app: order-postgres
    tier: data
    app.kubernetes.io/part-of: sneakerhead
    environment: prod
data:
  PGDATA: "/var/lib/postgresql/data/pgdata"
  POSTGRES_DB: "orderdb"
---
# Order PostgreSQL Secret - prod
apiVersion: v1
kind: Secret
metadata:
  name: order-postgres-secret
  namespace: sneakerhead-prod
  labels:
    app: order-postgres
    tier: data
    app.kubernetes.io/part-of: sneakerhead
    environment: prod
type: Opaque
data:
  # echo -n "sneaker" | base64
  POSTGRES_USER: c25lYWtlcg==
  # echo -n "sneakerpass123" | base64
  POSTGRES_PASSWORD: c25lYWtlcnBhc3MxMjM=
---
# Order PostgreSQL StatefulSet - prod
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: order-postgres
  namespace: sneakerhead-prod
  labels:
    app: order-postgres
    tier: data
    app.kubernetes.io/part-of: sneakerhead
    environment: prod
spec:
  serviceName: order-postgres
  replicas: 1
  selector:
    matchLabels:
      app: order-postgres
      tier: data
  template:
    metadata:
      labels:
        app: order-postgres
        tier: data
        app.kubernetes.io/part-of: sneakerhead
        environment: prod
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          imagePullPolicy: Always
          ports:
            - containerPort: 5432
              protocol: TCP
          envFrom:
            - configMapRef:
                name: order-postgres-config
            - secretRef:
                name: order-postgres-secret
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "1"
              memory: 1Gi
          volumeMounts:
            - name: order-postgres-data
              mountPath: /var/lib/postgresql/data
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - sneaker
                - -d
                - orderdb
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 5
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - sneaker
                - -d
                - orderdb
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
  volumeClaimTemplates:
    - metadata:
        name: order-postgres-data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: nfs-client
        resources:
          requests:
            storage: 4Gi
---
# Order PostgreSQL Headless Service - prod
apiVersion: v1
kind: Service
metadata:
  name: order-postgres
  namespace: sneakerhead-prod
  labels:
    app: order-postgres
    tier: data
    app.kubernetes.io/part-of: sneakerhead
    environment: prod
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: order-postgres
    tier: data
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
