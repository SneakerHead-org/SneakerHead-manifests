# Gateway for dev namespace - uses existing kgateway GatewayClass
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: sneakerhead-gateway
  namespace: sneakerhead-dev
  labels:
    app.kubernetes.io/name: sneakerhead-gateway
    app.kubernetes.io/part-of: sneakerhead
    environment: dev
spec:
  gatewayClassName: kgateway
  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: Same
